package io.floci.gcp.services.kafka.model;

/** {@code google.cloud.managedkafka.v1.Connector.State}. */
public enum ConnectorState {
    STATE_UNSPECIFIED,
    UNASSIGNED,
    RUNNING,
    PAUSED,
    FAILED,
    RESTARTING,
    STOPPED
}
